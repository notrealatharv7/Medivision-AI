name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *"   # runs every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render Service
        run: curl https://n8n-tj96.onrender.com/workflow/CoZhyP0GfQEAcKxug1qcv?projectId=9YyfRI1cwKThpf9v
